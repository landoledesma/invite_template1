---
/**
 * Componente que anima el fondo SVG cargado desde `/public`.
 */
---

<style>
  .background {
    position: fixed;
    top: 0;
    left: 0;
    width: 200%; /* Se amplía para un efecto de desplazamiento */
    height: 200%; 
    background: url('/Background.svg') no-repeat center center; /* Ruta al SVG */
    background-size: cover;
    animation: move-background 30s linear infinite;
    z-index: -1; /* Envía el fondo detrás del contenido */
  }

  @keyframes move-background {
    0% {
      transform: translate(0, 0);
    }
    50% {
      transform: translate(-25%, -25%);
    }
    100% {
      transform: translate(0, 0);
    }
  }
</style>

<div class="background"></div>
